# SoundScan / Phaselink 开发对话总结

日期：2026-08-15

本文档汇总本次开发会话的全部内容：环境配置、代码仓库、重构合并、实时数据链路、问题诊断与修复、当前状态与下一步。

---

## 一、项目概况与构建环境

### 项目结构

- `C:\SoundScan\phaselink_code` —— Phaselink 超声上位机（Qt Widgets，qmake 构建）
- `C:\SoundScan` —— SoundScan 总项目（含 phaselink_code 子目录、3DScan 子仓库、configwindow 参数配置窗口等）
- `C:\SoundScan\3DScan` —— 3D 点云渲染子仓库（独立 git 仓库，VTK + CUDA + OpenGL）
- `C:\Phaselink\SDK-1.0.0.10\phaselink_code` —— SDK 附带的旧版 phaselink 源码

### 构建环境

- Qt 6.8.3 MSVC2022_64（`C:\Qt\6.8.3\msvc2022_64`）
- Visual Studio 2022 Community（vcvars64.bat）
- jom 并行构建（`C:\Qt\Tools\QtCreator\bin\jom\jom.exe`）
- Phaselink SDK：`C:\Phaselink\include`、`C:\Phaselink\lib`
- 依赖：TwinCAT ADS、LibKuka3D、Python 3.13、VTK 9.6、CUDA 13.3

### 构建命令（Release，out-of-source）

```bat
call "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat"
cd /d "C:\SoundScan\build\Desktop_Qt_6_8_3_MSVC2022_64bit-Release"
"C:\Qt\6.8.3\msvc2022_64\bin\qmake.exe" "C:\SoundScan\SoundScan.pro" -spec win32-msvc CONFIG+=release
"C:\Qt\Tools\QtCreator\bin\jom\jom.exe" -j8
```

产物：`C:\SoundScan\build\Desktop_Qt_6_8_3_MSVC2022_64bit-Release\release\SoundScan.exe`

### 注意事项

- Debug 构建与 Phaselink SDK 不兼容：SDK DLL 使用 Release 版 Qt/CRT，Debug 程序跨 CRT 释放 `QVector<int>` 会在启动时崩溃（`is_block_type_valid` 断言）。**调试请使用带 PDB 的 Release 构建**（`CONFIG += force_debug_info`）。
- 编译时若 `SoundScan.exe` 被占用（残留进程），可通过“把旧 exe 改名”再链接；残留进程需在任务管理器/调试器里关闭。

---

## 二、VS Code 构建/调试配置

位置：`C:\SoundScan\phaselink_code\.vscode\`

- `tasks.json`：`Build: Debug`（默认）、`Build: Release`，内部调用 vcvars64 + qmake + jom
- `launch.json`：两个配置
  - `Phaselink (Build + Debug)`：构建并调试（Release 版，带 PDB）
  - `Phaselink (Build + Run)`：构建并运行
  - 两个配置都指向 `build-release\release\Phaselink.exe`，PATH 已加 `C:\Phaselink\lib`
- 用法：`F5` 调试、`Ctrl+F5` 运行、`Ctrl+Shift+B` 构建

> 注：`.vscode` 文件曾被外部同步改动过（配置名被改），已基于实际内容修正。

---

## 三、代码仓库与 GitHub 上传

GitHub 账号：`DrestryRobot`（SSH：`git@github.com:DrestryRobot/...`）

### 已推送内容

| 仓库 | 分支 | 内容 | 提交 |
|---|---|---|---|
| `DrestryRobot/phaselink_code.git` | `main` | `C:\Phaselink\SDK-1.0.0.10\phaselink_code` | `a4e8f8d` |
| `DrestryRobot/phaselink_code.git` | `better_phaselink` | `C:\SoundScan\phaselink_code`（含 .vscode、mainwindow3 等） | `452650f` |

### 仓库现状

- `C:\SoundScan\phaselink_code`：独立 git 仓库（better_phaselink 分支，有未提交改动）
- `C:\SoundScan`：git 仓库（SoundScan.pro 等，有未提交改动）
- `C:\SoundScan\3DScan`：独立 git 仓库（scan.cpp/mainwindow7.cpp 等有未提交改动）

### 已知现象

外部同步/工具会不定期覆盖工作区文件（.vscode、3DScan 下的 scan.cpp/mainwindow7.cpp 等曾被回退或并行修改），改动需要留意被覆盖。

---

## 四、代码合并与重构

### 1. mainwindow3 合并进 mainwindow（phaselink_code）

- `mainwindow3.cpp/h/ui` 的内容合并为 `mainwindow.cpp/h/ui`（类名统一为 `MainWindow`，采用 DataDispatch 数据分发架构）
- 保留了回放增强逻辑（`findClosestKey`、`m_playbackBeamCount`、spinFrame 帧号控制）
- 删除 `mainwindow3.cpp/h/ui` 三个文件
- `datadispatch.cpp/h` 复制到 `phaselink_code/` 并加入 `Phaselink.pro`

### 2. SoundScan 对应修改

- `SoundScan.pro`：`phaselink_code/mainwindow3.*` → `phaselink_code/mainwindow.*`；`datadispatch.*` → `phaselink_code/datadispatch.*`；`dialog/viewmodel.*` → `phaselink_code/dialog/viewmodel.*`；新增编译 `3DScan/scandata.cpp`
- 根目录“参数配置”窗口 `MainWindow` 重命名为 `ConfigWindow`（文件 `mainwindow.*` → `configwindow.*`），内部枚举 `ConState` → `ConfigState`，避免与合并后的 `MainWindow` 冲突
- `main.cpp`：`ConfigWindow w`（参数配置）+ `MainWindow w3`（调试扫描）+ `MainWindow5 w5`（扫描显示）
- `viewmodel.cpp/h` 最终位于 `phaselink_code\dialog\`

---

## 五、扫描控制联动（SoundScan ↔ 3DScan）

需求：SoundScan 的“扫描开始/暂停/结束”三个按钮，与 3DScan 的“开始/停止/结束绘制”联动；扫描开始**不立即**开始绘制，要等机器人开始扫板的信号。

### 实现

- `ConfigWindow::on_pushButton_9/19/20`（扫描开始/暂停/结束）是三个按钮的汇聚点
- 新增 `m_scanStartPending` 标志：扫描开始只置标志，不立即绘制
- 机器人开始信号 = PLC 变量 0 跳变（`start3DChecker` 每 10ms 轮询 `adsClient.getIntVal(0)`）
  - var0 变 0 → `requestStartDrawing` → `onRequestStartDrawing` → 若待命则调用 `MainWindow7::startDrawing()`
  - var0 变非 0 → `requestStopDrawing`（仅扫描进行中生效）
- `MainWindow7` 新增公共入口 `startDrawing() / pauseDrawing() / finishDrawing()`（内部调用原 private slots）
- 通过 `MainWindow5::s_instance->getMainWindow7()` 获取 3DScan 窗口

### 重要修复

var0 的开始/停止信号**只在“扫描进行中”生效**（`scan_continue_flag == false`），避免启动阶段/结束后 var0 跳变抢先给 PLC 发停止命令（曾导致机器人卡停）。

---

## 六、实时数据链路架构

### 数据源

- **机器人**：KUKA RSI（UDP，250Hz / IPOC 每 4ms）→ `udpserver.cpp` → 写 scandata 全局量 `robot_x/y/z/a/b/c/robot_ipoc`
- **超声**：Phaselink SDK（180Hz）→ `DataDispatch` → `MainWindow5::onDataPacket` → ViewModel 线程 → 写 scandata 全局量 `amp[64]/tof[64]/si/beam`

### scandata 全局量（单一事实来源）

`C:\SoundScan\3DScan\scandata.cpp/h`：

```cpp
double amp[64], tof[64], si; int beam;
double robot_x/y/z/a/b/c; quint32 robot_ipoc;
double longmen[2]; bool m_start;
```

- `scandata.cpp` 已加入 SoundScan.pro 编译，`scan.cpp` 中的重复定义已删除
- 其他文件只 `#include "3DScan/scandata.h"` 使用 extern 声明

### Scan 实时模式（3DScan）

- 无 CSV 时走实时模式：以机器人 IPOC 为硬件时间戳
- **1ms 定时器轮询** `robot_ipoc`（机器人每 4ms 变一次，1ms 采样不漏）
- 检测到 IPOC 变化才处理一帧：`sendNextFrame → 在线网格(processFrameOnline → emitGridCell → flushOnlinePack) → newFrameAvailable → MainWindow7::renderFrame → AddPointCloud（CUDA VBO 上传）→ VTK 渲染`
- 暂停恢复路径已修正：CSV 用 4ms 定时器、实时用 1ms 轮询
- 离线那套（CSV 回放 + `buildUniformCloud` + `m_gridCloud` 均匀网格播放）目前是死代码，仅手动加载 CSV 时启用

---

## 七、调试日志体系（全部输出到系统日志）

日志文件：`C:\超声扫描\日志\系统日志2026-08-15.txt`（按天）

| 标记 | 内容 | 频率 |
|---|---|---|
| `[ScanCtrl]` | 扫描开始/暂停/结束时的 PLC 命令写入与回读（0x5E256、0x5EB08、var0） | 事件 |
| `[3DChecker]` | var0 跳变、开始/停止信号触发、被忽略的记录 | 事件 |
| `[Robot]` | 机器人每秒帧数、IPOC、位姿 | 1 次/秒 |
| `[Scan] 实时轮询` | 1ms 轮询次数、robot_ipoc、amp/tof/si/beam | 1 次/秒 |
| `[Scan] IPOC停滞警告` | IPOC 超过 500ms 未变化（判断阻塞/卡死） | 出现时 1 次/秒 |
| `数据播放频率` | Scan 实际处理帧数（应稳定 250） | 1 次/秒 |
| `[PointCloud]` | GUI 收到帧数、点云点数 | 1 次/秒 |
| `[Grid]` | 网格单元发射/去重/在线单元总数 | 1 次/秒 |
| `[Ref]` | 每带参考（passIndex/passStepSign/pass0Ref/uRef/kc/ucS） | 1 次/秒 |
| `渲染帧率` | VTK 实际渲染帧率 | 1 次/秒 |
| `[GridStats]` | 扫描结束后每带 k/j 范围与点数（检查带间距） | 扫描结束 |

> 修复过的问题：`DebugOutput::messageHandler` 之前对每条日志调用 `QLineEdit::setText()`（跨线程操作 Qt 控件），导致 GUI 卡死/崩溃；现只写文件、不再更新控件。

---

## 八、问题诊断与修复记录

### 1. 机器人卡停（扫描开始后不动）

- **现象**：点击扫描开始后机器人不动，日志显示 var0 在启动阶段误触发停止命令
- **原因**：`start3DChecker` 在启动阶段把 var0 跳变当成停止信号，提前发了 `0x5E256=8`
- **修复**：var0 开始/停止信号只在扫描进行中生效；轮询间隔 1ms→10ms
- **验证**：机器人恢复 250Hz 运动（IPOC 连续增长）

### 2. GUI 卡死（日志停在 startDrawing 后）

- **现象**：日志停在 `[3DChecker] startDrawing 调用完成`，进程无响应
- **原因**：`DebugOutput` 消息处理器对每条日志调用 `QLineEdit::setText()`，跨线程操作 Qt 控件导致 Qt 文本引擎崩溃（`QTextEngine::shapeText` 访问非法内存）
- **修复**：消息处理器只写日志文件，不再更新界面控件

### 3. 点云带宽度异常（97 列，成对重叠）

- **现象**：`[GridStats]` 显示带 0 宽 49 列，带 1 以后宽 97 列且与上一条带重叠 49 列
- **原因**：换向检测延迟（`m_currentIndex - m_lastFlipFrame >= 300`，1.2s）+ 锚点逻辑未正确推进，导致新条带开头数据被放到旧条带位置
- **修复进展**：
  - 换向延迟 300→60 帧
  - 锚点逻辑外部迭代为“整条线平均横向中心”锚定（`m_prevPassCenterU + 14.4mm`，带 2mm 容差回退理想网格）
  - 最新日志确认 uRef 冻结正确（带 40/41 的 uRef 精确等于 `pass0Ref - N×14.4`）
- **待验证**：完整扫描的 GridStats 带间距是否为整齐 48 列、不再成对重叠

### 4. AMP/TOF 切换不同步

- **现象**：点 AMP/TOF 按钮，点云和曲面网格不同步切换
- **原因**：`isAmpMode` 在 `recolorSurfaceMesh()` 之后才翻转
- **修复**：先翻转 `isAmpMode`，再统一重着色点云和曲面网格

### 5. UI 数据帧率 < 250、不稳定

- **解释**：UI“数据帧率”= GUI 线程每秒实际处理的帧数（消费速率）；Scan 生产 250 帧/秒，但 GUI 每帧做 CUDA 点云上传 + VTK 渲染，处理能力约 180-230
- **验证**：IPOC 源本身健康（数据播放频率稳定 250、无停滞警告）
- **处理**：不做批量上传（用户要求逐帧渲染），改为显示抽稀提升渲染性能

### 6. 渲染卡顿、帧率低（8-16fps）

- **原因**：`glDrawArrays(GL_POINTS, 0, validPointCount)` 每帧全量绘制，点云到 500 万+ 点后 GPU 饱和
- **修复**：
  - 顶点着色器加 `u_stride` 显示抽稀：只绘制下标为 stride 倍数的点（数据完整保留）
  - stride 按点云规模动态计算（2 的幂、只增不减），目标显示 ≤120 万点
  - 交互（旋转/缩放）时再降为 1/3 点（`SetValidPointCount(cloudValidPoints/3)`）
  - 曲面网格更新从每 10 帧降到每 40 帧
- **预期**：渲染帧率稳定 30fps、交互流畅
- **待验证**：新构建（15:16）实测

---

## 九、当前状态与下一步

### 已完成

- VS Code 构建/调试配置
- 代码合并与 GitHub 推送（main + better_phaselink）
- 扫描按钮 ↔ 3DScan 绘制联动
- 实时数据链路（机器人 250Hz + 超声 180Hz → scandata）
- 1ms IPOC 轮询 + 停滞检测
- 机器人卡停、GUI 冻结、AMP/TOF 切换、带宽度等已修复
- 渲染显示抽稀（目标 30fps）

### 待验证/待办

1. 完整扫描验证 `[GridStats]` 带间距（48 列、无成对重叠）
2. 验证渲染帧率稳定 30fps、交互流畅（新构建 15:16）
3. 验证 AMP/TOF 整体切换
4. 三个仓库（phaselink_code / SoundScan / 3DScan）的未提交改动需要分别 commit/push
5. 若显示抽稀后点云观感太疏/太密，调整显示预算（当前 120 万点）

---

## 附录：关键文件路径

- `C:\SoundScan\phaselink_code\Phaselink.pro` —— phaselink 工程
- `C:\SoundScan\SoundScan.pro` —— SoundScan 工程
- `C:\SoundScan\configwindow.cpp/h` —— 参数配置窗口（扫描按钮、PLC 命令、3D 检查）
- `C:\SoundScan\udpserver.cpp` —— 机器人 RSI/IPOC 接收
- `C:\SoundScan\mainwindow5.cpp/h` —— 扫描显示窗口（A 扫、超声分发、ViewModel）
- `C:\SoundScan\phaselink_code\dialog\viewmodel.cpp/h` —— 超声数据 → scandata + LibKuka3D
- `C:\SoundScan\phaselink_code\dialog\dataprocessor.cpp/h` —— 超声帧分发
- `C:\SoundScan\phaselink_code\dialog\viewwidget.cpp/h` —— A 扫/E 扫/C 扫视图
- `C:\SoundScan\3DScan\scan.cpp/h` —— 3D 数据采集/在线网格/IPOC 轮询
- `C:\SoundScan\3DScan\scandata.cpp/h` —— 共享全局量唯一来源
- `C:\SoundScan\3DScan\mainwindow7.cpp/h` —— 3D 点云窗口（渲染、AMP/TOF、交互）
- `C:\SoundScan\3DScan\vtkvboactor.cpp/h` —— 点云 VBO 渲染（显示抽稀）
- `C:\SoundScan\debugoutput.cpp` —— 系统日志写入
- `C:\超声扫描\日志\系统日志2026-08-15.txt` —— 当日系统日志

